import string


def fn_str(str):
    m,n = 0
    if str in string.ascii_letters:
        return 'ascii_letters'  # ASCII 字母
        if str in string.ascii_uppercase:
            n += n
        else: str in string.ascii_lowercase:
                m += m
    else:
        return '-1'
